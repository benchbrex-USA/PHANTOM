use super::{Skill, SkillId, SkillCategory, SkillComplexity, OutputFormat, RetryStrategy, SkillRegistry};
use crate::agents::AgentRole;

pub fn register(_registry: &mut SkillRegistry) {
    // Skills registered by agent — pending full implementation
}
